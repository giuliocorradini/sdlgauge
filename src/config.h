/*
 *  Constants for various calculations
 */

#pragma once

const int WINDOW_HEIGHT = 500;  /*< the height of the window */
const int WINDOW_WIDTH = 500;   /*< the width of the window */
const float max_rpm = 6000;     /*< full scale RPMs */

const int power = 10;           /*< speed of gas rising when pressing the spacebar */

const int MAX_SPEED = 75;       /*< speed in km/h corresponding to full scale rpms */
